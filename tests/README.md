# Validation coverage

Run the browser validation with `npm run test:e2e` after a successful production build.

The Playwright suite verifies source content and order, durable local assets and fonts, accessibility, the supplied desktop/tablet/mobile compositions, responsive transition widths, and evidenced visual tokens. Reference-width and transition-adjacent screenshots are attached to the GitHub Actions validation artifact for review.
